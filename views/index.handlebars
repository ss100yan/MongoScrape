

<div class="row">
  <div class="col s1"></div>
  <div class ="col s12 left-align">
    {{#each article}}
      <div class="card-panel">
        <h4>{{this.title}}</h4>
        <a href="{{this.link}}">{{this.link}}</a>
       <a class="waves-effect waves-light btn-small "><i class="material-icons left ">save</i></a>
            </div>
    {{/each}}
  </div>
</div>