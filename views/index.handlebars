

<div class="row">
  <div class="col s1"></div>
  <div class ="col s12 left-align">
    {{#each article}}
      <div class="card-panel">
        <h4>{{this.title}}</h4>
        <a href="{{this.link}}">{{this.link}}</a>
       <a class="waves-effect waves-light btn-small orange" href="/save"><i class="material-icons left ">save</i>Save</a>
            </div>
    {{/each}}
  </div>
</div>